apiVersion: v1
kind: Service
metadata:
  name: calculator-api-service
  namespace: calculator-namespace
spec:
  selector:
    app: calculator-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3333
  type: ClusterIP
